/**
 * Type definition for GIF files so we can import them and the TS
 * compiler doesn't complain about them
 */
declare module "*.gif" {
  const filePath: string;
  export default filePath;
}

/**
 * Type definition for SVG files so we can import them and the TS
 * compiler doesn't complain about them
 */
declare module "*.svg" {
  const filePath: string;
  export default filePath;
}

/**
 * Type definition for PNG files so we can import them and the TS
 * compiler doesn't complain about them
 */
declare module "*.png" {
  const filePath: string;
  export default filePath;
}

/**
 * Type definition for JPG files so we can import them and the TS
 * compiler doesn't complain about them
 */
declare module "*.jpg" {
  const filePath: string;
  export default filePath;
}

/**
 * Type definition for JPEG files so we can import them and the TS
 * compiler doesn't complain about them
 */
declare module "*.jpeg" {
  const filePath: string;
  export default filePath;
}
